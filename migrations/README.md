# D1 migrations

Apply explicitly with `npx wrangler d1 migrations apply learn-to-type --remote` only after replacing the database UUID placeholder with the verified existing or newly created D1 UUID.
