import{useState}from'react';import{api}from'../api/client';import{CHARACTERS}from'../components/characters/characterCatalog';
export function CreatePlayerScreen({onBack,onCreated}:{onBack:()=>void;onCreated:()=>void}){const[name,setName]=useState('');const[character,setCharacter]=useState('runner_01');const[error,setError]=useState('');const create=async(e:React.FormEvent)=>{e.preventDefault();try{await api('/profiles',{method:'POST',body:JSON.stringify({display_name:name,character_id:character})});onCreated()}catch{setError('Could not create your player. Check the name and try again.')}};return <main className="form-screen create-player"><button className="quiet" onClick={onBack}>← Main menu</button><form onSubmit={create}><p className="eyebrow">Player setup</p><h1>Create player</h1><label>Display name<input autoFocus value={name} maxLength={40} required onChange={e=>setName(e.target.value)}/></label><fieldset><legend>Character</legend><div className="character-picker">{CHARACTERS.map(item=><button type="button" key={item.id} aria-pressed={character===item.id} onClick={()=>setCharacter(item.id)}><img src={item.image} alt=""/><span><b>{item.name}</b><small>{item.trait}</small></span></button>)}</div></fieldset><button>Create player</button>{error&&<p className="warning" role="alert">{error}</p>}</form></main>}
