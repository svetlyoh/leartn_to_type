import{useCallback,useEffect,useMemo,useRef,useState}from'react';
import{createState,applyInput,pause,resume}from'../typing-core/engine';
import type{TypingState}from'../typing-core/types';
import{accuracy,cadenceScore,grossWpm}from'../typing-core/metrics';
import{VisualKeyboard}from'../components/training/VisualKeyboard';
import{HandGuide}from'../components/training/HandGuide';
import{TrainingConsole}from'../components/ai/TrainingConsole';
import{coachNote,type CoachMode}from'../components/coach/coachRules';
import{CURRICULUM}from'../generated/curriculum.generated';
import{ModuleProgress,type ModuleProgressData}from'../components/training/ModuleProgress';
import{SessionControls}from'../components/training/SessionControls';
import{loadActive,saveActive,type SavedSession}from'../persistence/activeSessionStore';
import{api}from'../api/client';
import{useTrainingShortcuts}from'../hooks/useTrainingShortcuts';
import{TRAINING_SHORTCUTS}from'../config/shortcuts';

type LessonSource=SavedSession['source'];
type GeneratedLesson={lesson_id:string;text:string;lesson_kind:'drill'|'passage';source:'ai'|'cache'|'fallback';estimated_duration_seconds:number|null};

export function TrainingScreen({profileId,onExit,onClosed}:{profileId:string;onExit:()=>void;onClosed:()=>void}){
 const lessons=CURRICULUM.lessons;const[lessonIndex,setLessonIndex]=useState(0);const authored=lessons[lessonIndex];const[lessonId,setLessonId]=useState<string>(authored.id);const[stageId,setStageId]=useState<string>(authored.stageId);const[source,setSource]=useState<LessonSource>('authored');const[state,setState]=useState<TypingState>(()=>createState(authored.text));const[mode,setMode]=useState<CoachMode>('calm');const[hideMetrics,setHideMetrics]=useState(false);const[consoleOpen,setConsoleOpen]=useState(false);const[saveStatus,setSaveStatus]=useState('');const[exitOpen,setExitOpen]=useState(false);const[reshuffleOpen,setReshuffleOpen]=useState(false);const[loading,setLoading]=useState(false);const[pending,setPending]=useState<GeneratedLesson|null>(null);const[moduleProgress,setModuleProgress]=useState<ModuleProgressData|null>(null);const surface=useRef<HTMLElement>(null);const activeCharacter=useRef<HTMLSpanElement>(null);const hydrated=useRef(false);const reduceMotion=matchMedia('(prefers-reduced-motion: reduce)').matches;const passageMode=state.prompt.length>220;

 useEffect(()=>{Promise.all([loadActive(profileId),api<ModuleProgressData>('/module-progress')]).then(([saved,progress])=>{if(saved?.saveVersion===1){const current=performance.now();setLessonIndex(Math.min(saved.lessonIndex,lessons.length-1));setLessonId(saved.lessonId);setStageId(saved.stageId);setSource(saved.source);setState({...saved.typingState,startedAt:saved.typingState.startedAt===null?null:current-saved.typingState.activeMs,lastCorrectAt:null,pausedAt:null});setSaveStatus(saved.pendingSync?'Saved on this device · sync pending':'Saved session restored')}else{const index=lessons.findIndex(item=>item.stageId===progress.stage_id);if(index>=0){setLessonIndex(index);setLessonId(lessons[index].id);setStageId(lessons[index].stageId);setState(createState(lessons[index].text))}}setModuleProgress(progress)}).catch(()=>setSaveStatus('Local training is ready')).finally(()=>{hydrated.current=true;setTimeout(()=>surface.current?.focus(),0)})},[lessons,profileId]);
 useEffect(()=>{if(passageMode&&state.position>0&&consoleOpen)setConsoleOpen(false)},[consoleOpen,passageMode,state.position]);
 useEffect(()=>{if(passageMode)activeCharacter.current?.scrollIntoView({block:'center',behavior:reduceMotion?'auto':'smooth'})},[passageMode,reduceMotion,state.position]);
 useEffect(()=>{const key=(e:KeyboardEvent)=>{if(e.key==='F1'||e.key==='F2'||(e.shiftKey&&e.key==='Enter'))return;const target=e.target as HTMLElement|null;if(target?.matches('input,button,select,textarea,[contenteditable="true"]'))return;e.preventDefault();setState(s=>applyInput(s,{key:e.key,code:e.code,time:performance.now(),repeat:e.repeat,ctrlKey:e.ctrlKey,metaKey:e.metaKey,altKey:e.altKey}))};const blur=()=>setState(s=>pause(s,performance.now()));const focus=()=>{if(!consoleOpen)setState(s=>resume(s,performance.now()))};addEventListener('keydown',key);addEventListener('blur',blur);addEventListener('focus',focus);return()=>{removeEventListener('keydown',key);removeEventListener('blur',blur);removeEventListener('focus',focus)}},[consoleOpen]);

 const metrics=useMemo(()=>{const usable=state.intervals.filter(value=>value>0&&value<=4000);return{wpm:grossWpm(state.correctChars,state.activeMs),accuracy:accuracy(state.correctChars,state.attempts),cadence:usable.length>=12?cadenceScore(usable):null}},[state]);
 const makeSaved=useCallback((pendingSync:boolean,checkpointId:string):SavedSession=>({id:profileId,saveVersion:1,lessonId,lessonIndex,stageId,typingState:pause(state,performance.now()),source,updatedAt:new Date().toISOString(),pendingSync,localCheckpointId:checkpointId}),[lessonId,lessonIndex,profileId,source,stageId,state]);
 const checkpoint=useCallback(async()=>{setState(current=>pause(current,performance.now()));const checkpointId=`chk_${crypto.randomUUID()}`;const saved=makeSaved(true,checkpointId);await saveActive(saved);const payload={save_version:1,curriculum_version:CURRICULUM.version,stage_id:stageId,lesson_id:lessonId,char_index:state.position,round_index:lessonIndex+1,elapsed_active_ms:state.activeMs,difficulty:'practice',weak_keys:[],local_checkpoint_id:checkpointId};try{await api('/session-checkpoint',{method:'POST',body:JSON.stringify(payload)});await saveActive({...saved,pendingSync:false});setSaveStatus('Saved')}catch{setSaveStatus('Saved on this device · sync pending')}},[lessonId,lessonIndex,makeSaved,stageId,state.activeMs,state.position]);
 useEffect(()=>{if(!hydrated.current)return;const timer=setInterval(()=>void checkpoint(),45000);return()=>clearInterval(timer)},[checkpoint]);
 useEffect(()=>{if(state.complete&&hydrated.current)void checkpoint()},[checkpoint,state.complete]);
 useEffect(()=>{const hidden=()=>{if(document.visibilityState==='hidden')void checkpoint()};const pagehide=()=>{const checkpointId=`chk_${crypto.randomUUID()}`;void saveActive(makeSaved(true,checkpointId))};document.addEventListener('visibilitychange',hidden);addEventListener('pagehide',pagehide);return()=>{document.removeEventListener('visibilitychange',hidden);removeEventListener('pagehide',pagehide)}},[checkpoint,makeSaved]);

 const next=useCallback(()=>{if(loading)return;const index=(lessonIndex+1)%lessons.length;setLessonIndex(index);setLessonId(lessons[index].id);setStageId(lessons[index].stageId);setSource('authored');setState(createState(lessons[index].text));setPending(null);setSaveStatus('');setTimeout(()=>surface.current?.focus(),0)},[lessonIndex,lessons,loading]);
 const requestLesson=useCallback(async(action='reshuffle',request='')=>{if(action==='continue'){setConsoleOpen(false);setTimeout(()=>surface.current?.focus(),0);return}if(loading)return;setLoading(true);try{const topic=request.toLowerCase().includes('running')?'running':request.slice(0,60);const target=action==='custom_passage'?Math.max(80,state.prompt.length):Math.max(20,Math.min(state.prompt.length,160));const generated=await api<GeneratedLesson>('/ai/lesson',{method:'POST',body:JSON.stringify({request_id:crypto.randomUUID(),schema_version:1,curriculum_version:CURRICULUM.version,stage_id:stageId,mode:action,difficulty:'practice',target_characters:target,topic})});setPending(generated);setSaveStatus(generated.source==='fallback'?'Built-in practice prepared':'New practice prepared')}catch{setSaveStatus('Built-in training is still available')}finally{setLoading(false);setConsoleOpen(true)}},[loading,stageId,state.prompt.length]);
 const startPending=()=>{if(!pending)return;setLessonId(pending.lesson_id);setSource(pending.source);setState(createState(pending.text));setPending(null);setConsoleOpen(false);setTimeout(()=>surface.current?.focus(),0)};
 const reshuffle=useCallback(()=>{if(state.position>0&&!state.complete)setReshuffleOpen(true);else void requestLesson('reshuffle')},[requestLesson,state.complete,state.position]);
 const toggleConsole=useCallback(()=>{setConsoleOpen(value=>{const nextOpen=!value;setState(current=>nextOpen?pause(current,performance.now()):resume(current,performance.now()));if(!nextOpen)setTimeout(()=>surface.current?.focus(),0);return nextOpen})},[]);const back=useCallback(()=>{setConsoleOpen(false);setState(current=>resume(current,performance.now()));setTimeout(()=>surface.current?.focus(),0)},[]);useTrainingShortcuts({canNext:state.complete&&!loading,consoleOpen,onNext:next,onToggleConsole:toggleConsole,onReshuffle:reshuffle,onReturn:back});
 const saveExit=async()=>{await checkpoint();onExit()};const close=async()=>{await checkpoint();try{await api('/auth/profile-exit',{method:'POST',body:'{}'})}catch{/* local checkpoint is already safe */}window.close();onClosed()};

 return <main ref={surface} tabIndex={-1} className={`${consoleOpen?'console-mode ':''}${passageMode?'passage-mode':''}`}><header className="topbar"><div><span className="mark">C</span><strong>Cadence</strong></div><SessionControls onSave={()=>void checkpoint()} onExit={()=>setExitOpen(true)} onClose={()=>void close()} status={saveStatus}/><div className="controls"><label>Coach <select value={mode} onChange={event=>setMode(event.target.value as CoachMode)}><option value="silent">Silent</option><option value="calm">Calm</option><option value="competitive">Competitive</option></select></label><label><input type="checkbox" checked={hideMetrics} onChange={event=>setHideMetrics(event.target.checked)}/> Hide metrics</label></div></header><div className="shell"><section className="lesson"><ModuleProgress data={moduleProgress}/><p className="eyebrow">Round {lessonIndex+1} of {lessons.length} · technique first</p><h1>{authored.title}</h1><p className="instruction">Keep your wrists relaxed. Find the active key, then keep your eyes on the screen.</p>{!hideMetrics&&<div className="metrics"><span><b>{state.correctChars<10?'—':metrics.wpm.toFixed(0)}</b> WPM</span><span><b>{state.attempts?metrics.accuracy.toFixed(0):'—'}</b> accuracy</span><span><b>{metrics.cadence?.toFixed(0)??'—'}</b> cadence<small>{metrics.cadence===null?'collecting':''}</small></span></div>}<div className="prompt" aria-label="Typing prompt">{[...state.prompt].map((character,index)=><span ref={index===state.position?activeCharacter:undefined} key={index} className={index<state.position?'done':index===state.position?'current':''}>{character===' '?'·':character}</span>)}</div><p className="position">Round progress · {state.position} / {state.prompt.length} characters</p><VisualKeyboard active={state.prompt[state.position]} error={state.lastErrorKey??undefined}/><HandGuide target={state.prompt[state.position]??''} show={state.repeatErrors>=2} reduceMotion={reduceMotion}/>{state.complete&&<div className="complete"><h2>Round complete</h2><p>{coachNote(mode,metrics.accuracy,metrics.wpm)}</p><button onClick={next} disabled={loading} title={`Next training round (${TRAINING_SHORTCUTS.nextRound})`} aria-keyshortcuts="Shift+Enter">Next training round <kbd>Shift + Enter</kbd></button></div>}</section><TrainingConsole open={consoleOpen} onToggle={toggleConsole} onAction={(action,request)=>void requestLesson(action,request)} moduleTitle={moduleProgress?.title??'Current module'} modulePercent={moduleProgress?.progress_percent??0}/></div>{pending&&<div className="modal" role="dialog" aria-modal="true"><div><p className="eyebrow">Validated practice ready</p><h2>{pending.lesson_kind==='passage'?'Your passage is ready':'Your drill is ready'}</h2><p>{pending.text.length} characters, kept inside your current key set. Your current round stays in place until you start.</p><button onClick={startPending}>Start {pending.lesson_kind}</button><button onClick={()=>void requestLesson('reshuffle')}>Reshuffle again</button><button className="quiet" onClick={()=>setPending(null)}>Keep current lesson</button></div></div>}{exitOpen&&<div className="modal" role="dialog" aria-modal="true"><div><h2>Exit training?</h2><p>Your current round will be saved so you can resume it later.</p><button onClick={()=>void saveExit()}>Save &amp; Exit</button><button className="quiet" onClick={()=>setExitOpen(false)}>Keep training</button></div></div>}{reshuffleOpen&&<div className="modal" role="dialog" aria-modal="true"><div><h2>Reshuffle this round?</h2><p>Your current round is not finished.</p><button onClick={()=>{setReshuffleOpen(false);void requestLesson('reshuffle')}}>Prepare reshuffle</button><button className="quiet" onClick={()=>setReshuffleOpen(false)}>Keep current</button></div></div>}</main>;
}
