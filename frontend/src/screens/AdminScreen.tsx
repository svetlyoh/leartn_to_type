import{useState}from'react';import{api}from'../api/client';import{CHARACTERS}from'../components/characters/characterCatalog';
export function AdminScreen({authenticated,onAuthenticated,onBack}:{authenticated:boolean;onAuthenticated:()=>void;onBack:()=>void}){const[pin,setPin]=useState('');const[name,setName]=useState('');const[profilePin,setProfilePin]=useState('');const[character,setCharacter]=useState('runner_01');const[message,setMessage]=useState('');const login=async(e:React.FormEvent)=>{e.preventDefault();try{await api('/auth/admin-login',{method:'POST',body:JSON.stringify({pin})});onAuthenticated()}catch{setMessage('Access unavailable. Check the PIN or try again later.')}};const create=async(e:React.FormEvent)=>{e.preventDefault();try{await api('/profiles',{method:'POST',body:JSON.stringify({display_name:name,pin:profilePin||undefined,character_id:character})});setMessage(`Created ${name}.`);setName('');setProfilePin('')}catch{setMessage('Could not create that profile. Check the fields and try again.')}};return <main className="form-screen"><button className="quiet" onClick={onBack}>← Main menu</button>{!authenticated?<form onSubmit={login}><p className="eyebrow">Admin / Test mode</p><h1>Admin access</h1><label>Admin PIN<input autoFocus type="password" inputMode="numeric" value={pin} onChange={e=>setPin(e.target.value)}/></label><button>Unlock admin</button></form>:<form onSubmit={create}><p className="eyebrow">Profile management</p><h1>Create player</h1><label>Display name<input value={name} maxLength={40} required onChange={e=>setName(e.target.value)}/></label><label>Optional profile PIN<input value={profilePin} type="password" inputMode="numeric" minLength={4} maxLength={6} onChange={e=>setProfilePin(e.target.value)}/></label><label>Character<select value={character} onChange={e=>setCharacter(e.target.value)}>{CHARACTERS.map(c=><option key={c.id} value={c.id}>{c.name} · {c.trait}</option>)}</select></label><button>Create player</button></form>}{message&&<p className="notice" role="status">{message}</p>}</main>}
