import{describe,it,expect}from'vitest';import{applyInput,createState,pause,resume}from'./engine';import{accuracy,cadenceCv,grossWpm}from'./metrics';import{keyMastery}from'./mastery';
describe('typing engine',()=>{it('advances only for correct input and keeps historical errors',()=>{let s=createState('a');s=applyInput(s,{key:'x',code:'KeyX',time:0});expect(s.position).toBe(0);expect(s.errors).toBe(1);s=applyInput(s,{key:'Backspace',code:'Backspace',time:2});expect(s.errors).toBe(1);s=applyInput(s,{key:'a',code:'KeyA',time:1000});expect(s.position).toBe(1);expect(s.complete).toBe(true)});it('ignores held keys and modifier shortcuts',()=>{let s=createState('a');s=applyInput(s,{key:'a',code:'KeyA',time:0,repeat:true});s=applyInput(s,{key:'a',code:'KeyA',time:1,ctrlKey:true});expect(s.attempts).toBe(0)});it('handles capitals, punctuation and stalls',()=>{let s=createState('A;');s=applyInput(s,{key:'a',code:'KeyA',time:0});expect(s.position).toBe(0);s=applyInput(s,{key:'A',code:'KeyA',time:2});s=applyInput(s,{key:';',code:'Semicolon',time:602});expect(s.complete).toBe(true);expect(s.stalls).toBe(1)});it('excludes hidden focus interval',()=>{let s=createState('ab');s=applyInput(s,{key:'a',code:'KeyA',time:0});s=pause(s,100);s=resume(s,1100);s=applyInput(s,{key:'b',code:'KeyB',time:1200});expect(s.intervals).toEqual([200])})});
describe('exact metrics',()=>{it('computes WPM and accuracy',()=>{expect(grossWpm(50,60000)).toBe(10);expect(accuracy(9,10)).toBe(90)});it('computes cadence CV',()=>expect(cadenceCv([100,100,100])).toBe(0));it('does not max mastery after one key',()=>expect(keyMastery({attempts:1,correct:1,errors:0,totalReactionMs:300})).toBeLessThan(20))});
