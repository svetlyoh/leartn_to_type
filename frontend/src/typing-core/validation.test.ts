import{describe,it,expect}from'vitest';import{validateGenerated}from'./validation';describe('AI validation',()=>{it('accepts constrained text',()=>expect(validateGenerated('asdf asdf asdf','asdf ',5).valid).toBe(true));it('rejects forbidden characters',()=>expect(validateGenerated('asdf q asdf','asdf ',5).errors).toContain('forbidden_character'));it('strips safe fences',()=>expect(validateGenerated('```text\nasdf asdf\n```','asdf ',5).text).toBe('asdf asdf'))});
